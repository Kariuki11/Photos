import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "AI Virtual Try-On",
  description: "Upload your model and clothing photos to generate AI-powered virtual try-on images",
  keywords: ["AI", "virtual try-on", "fashion", "clothing", "model"],
  authors: [{ name: "Kenneth Kariuki" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
